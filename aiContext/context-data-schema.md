# context-data-schema.md v1.0
Расширяет глобальный CONTEXT.md v1.7
Создан: 2026-05-15 14:30 MSK
Статус: черновик (задача для чата `9-data-schema`)

---

## 🎯 Зона ответственности этого контекста

- **Централизованное описание всех сущностей** и их полей
- **Связи между сущностями** (ForeignKey, ссылки, каскадные операции)
- **Типы данных** и их сериализация (kotlinx.serialization)
- **Индексы и ограничения целостности** в Firestore
- **Миграции схемы** при изменениях

**Что НЕ входит:** UI, бизнес-логика, репозитории, аутентификация, подробные схемы данных.

---

## 📌 Принцип работы с общей схемой

**Правило:** Если поле есть в общей схеме, детальный чат не может его изменить без обновления `9-data-schema`.

Информация для LLM в смежных контекстах
- При появлении нового поля в детальном чате — проверь, не противоречит ли оно - общей схеме
- Если схема в детальном чате устарела — напомни пользователю обновить context-data-schema.md
- Типы данных должны быть совместимы с kotlinx.serialization и Firestore
- Firestore не поддерживает настоящие Foreign Keys, целостность — на совести кода


---

## 🧩 Текущее состояние схемы (что нужно определить)

### 1. Корневые сущности

| Сущность | Ответственный чат | Статус |
| :--- | :--- | :--- |
| `User` | `2-user-environment` | ✅ есть черновик |
| `UserSettings` | `2-user-environment` | ✅ есть черновик |

### 2. Основные сущности инвентаризации

| Сущность | Ответственный чат | Статус |
| :--- | :--- | :--- |
| `Item` | `3-item` | ❌ не определено |
| `Category` | `3-item` / `4-classes` | ❌ не определено |
| `Location` | `3-item` / `5-exchange` | ❌ не определено |
| `Tag` | `4-classes` | ❌ не определено |

### 3. Сущности учёта перемещений

| Сущность | Ответственный чат | Статус |
| :--- | :--- | :--- |
| `Loan` (взял/дал) | `5-exchange` | ❌ не определено |
| `Transfer` (перемещение) | `5-exchange` | ❌ не определено |
| `Sale` / `Gift` | `5-exchange` | ❌ не определено |

### 4. Сущности аналитики и AI

| Сущность | Ответственный чат | Статус |
| :--- | :--- | :--- |
| `UsageEvent` | `6-ranking` | ❌ не определено |
| `MaintenanceTask` | `7-ai-advice` | ❌ не определено |
| `AIPrediction` | `7-ai-advice` | ❌ не определено |

---

## 🔗 Связи между сущностями (предварительно)
User (1) ──────< (n) Item
User (1) ──────< (n) Loan
User (1) ──────< (n) Transfer

Item (1) ──────< (n) Loan
Item (1) ──────< (n) Transfer
Item (1) ──────< (n) UsageEvent
Item (1) ──────< (n) MaintenanceTask

Category (1) ──< (n) Item
Location (1) ──< (n) Item (текущее местоположение)
Location (1) ──< (n) Transfer (история)

Tag (n) ──────> (n) Item (многие-ко-многим)

---


## ❓ Вопросы, которые нужно решить в чате `9-data-schema`

0. решение по /Users/User/AndroidStudioProjects/stuff/shared/src/commonMain/kotlin/top/smartable/stuffit/model -- структура файлов
1. **Формат идентификаторов:** Firebase auto-id (`String`) или числовые ID?
2. **Типы Timestamp:** `kotlinx.datetime.Instant` или `com.google.firebase.Timestamp`?
3. **Каскадные операции:** При удалении `Item` — удалять связанные `Loan`, `Transfer` или помечать как `deleted=true`?
4. **Версионирование схемы:** Поле `schemaVersion` в каждом документе или отдельная коллекция `_metadata`?
5. **Ограничения Firestore:** Документы до 1MB, ограничения на составные индексы. Как это влияет на связи?
6. **Denormalization:** Дублировать ли `itemName` в `Loan` для быстрых запросов или всегда делать join на клиенте?
7. **Коллекции vs подколлекции:** `users/{userId}/items` или отдельная коллекция `items` с полем `ownerId`?
8. Варианты классификации для `Item`, разнообразие классификационных подходов
9. Подходы к поиску и групповым операциям
10. Дорожная карта усложнения схемы данных (когда подключать графику, когда AI и т.п.)

---

## 📝 Формат описания сущности (шаблон для примера)

```kotlin
/**
 * [Название сущности]
 * Коллекция: [путь в Firestore]
 * Документ ID: [тип ID]
 */
@Serializable
data class EntityName(
    // Идентификатор
    val id: String,
    
    // Ссылки на другие сущности
    val ownerId: String,  // ссылка на User
    
    // Основные поля
    val name: String,
    val createdAt: Instant,
    val updatedAt: Instant,
    
    // Состояние
    val isDeleted: Boolean = false,  // soft delete
    
    // Вложенные объекты
    val metadata: Metadata?
)