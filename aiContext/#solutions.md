# Решения

260517
## ❓ Вопросы, которые нужно решить в чате `9-data-schema`

0. решение по /Users/User/AndroidStudioProjects/stuff/shared/src/commonMain/kotlin/top/smartable/stuffit/model -- структура файлов
1. **Формат идентификаторов:** Firebase auto-id (`String`) или числовые ID?
2. **Типы Timestamp:** `kotlinx.datetime.Instant` или `com.google.firebase.Timestamp`?
3. **Каскадные операции:** При удалении `Item` — удалять связанные `Loan`, `Transfer` или помечать как `deleted=true`?
4. **Версионирование схемы:** Поле `schemaVersion` в каждом документе или отдельная коллекция `_metadata`?
5. **Ограничения Firestore:** Документы до 1MB, ограничения на составные индексы. Как это влияет на связи?
6. **Denormalization:** Дублировать ли `itemName` в `Loan` для быстрых запросов или всегда делать join на клиенте?
7. **Коллекции vs подколлекции:** `users/{userId}/items` или отдельная коллекция `items` с полем `ownerId`?
8. Варианты классификации для `Item`, разнообразие классификационных подходов
9. Подходы к поиску и групповым операциям
10. Дорожная карта усложнения схемы данных (когда подключать графику, когда AI и т.п.)
---

## 2. Чек-ап задач по чату 9-data-schema

|№ |  | Вопрос | Решение (по нашей дискуссии) | Статус |
|0 | Структура файлов | shared/.../model/ — плоская, 8 файлов (UserAccount, Person, Area, Asset, Class, Document, Role, Consumable) | ✅ решено|
|1 | Формат ID | Firebase auto-id (String) | ✅ решено|
|2 | Типы Timestamp | kotlinx.datetime.Instant (KMP-совместим) | ✅ решено|
|3 | Каскадные операции | При удалении Asset — порциями (batch до 500) удалять связанные сущности. Мягкое удаление (isDeleted) — пока не нужно | ✅ решено|
|4 | Версионирование схемы | Отложено (на потом, когда пойдут миграции) | ⏸ отложено|
|5 | Ограничения Firestore | 1MB — хватит (у нас нет больших вложенных полей). Индексы — настраиваем под каждый whereEqualTo / составной запрос | ✅ учтено|
|6 | Денормализация | В MVP минимизируем. itemName в Role пока не дублируем. Если станет узким местом — добавим позже | ⏸ отложено|
|7 | Коллекции vs подколлекции | Отдельные коллекции + поле ownerId. Подколлекции — только для совсем приватных/независимых данных | ✅ решено|
|8 | Классификация Item | Решено через сущность Класс + СвязьВещиСКлассом + АтрибутыКлассов (EAV-подход для пользовательских классификаторов) | ✅ решено|
|9 | Поиск и групповые операции | Текстовый поиск — whereGreaterThanOrEqualTo/whereLessThan (префиксный). Полнотекстовый (Enterprise) — позже. Групповые операции — на клиенте или через облачные функции | ⏸ отложено|
|10 | Дорожная карта | Этап 1 (MVP): ядро (Person, Asset, Area, Document). Этап 2: Классификация + атрибуты. Этап 3: AI, графика, TrustedCircle | ✅ обозначено |

---
